# Guide

A nested reference file. The fixture ships one so that the tests exercise a
path with a directory in it. Every other shipped path is flat, and a class of
failure lives only in the directory component: a user file sitting at a name
the skill ships as a directory, and a release that turns a shipped file into
a directory.
