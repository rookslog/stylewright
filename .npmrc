# Turn an engine mismatch into an error instead of a warning.
#
# Without this, npm prints EBADENGINE and carries on, so a dependency that
# requires more Node than we advertise installs quietly and CI stays green. The
# CI matrix tests the exact floor named in package.json, and this is what makes
# that test able to fail.
engine-strict=true
